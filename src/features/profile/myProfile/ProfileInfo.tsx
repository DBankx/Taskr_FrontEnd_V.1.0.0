import React from "react";
import {observer} from "mobx-react-lite";

const ProfileInfo = () => {
    return (
        <div>
                
        </div>
    )
}

export default observer(ProfileInfo);